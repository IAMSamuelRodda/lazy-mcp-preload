{
  "tools": {
    "cloudflare_delete_kv": {
      "description": "Delete key from KV namespace. Irreversible.\n\n    Args:\n        params: DeleteKVInput with namespace ID and key\n\n    Returns:\n        Confirmation of deletion\n    ",
      "maps_to": "cloudflare_delete_kv",
      "server": "cloudflare-full",
      "inputSchema": {
        "properties": {
          "params": {
            "$ref": "#/$defs/DeleteKVInput"
          }
        },
        "required": [
          "params"
        ],
        "type": "object"
      }
    }
  }
}
