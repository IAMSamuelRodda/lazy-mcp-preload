{
  "tools": {
    "cloudflare_get_worker": {
      "description": "Get Worker script content by name.\n\n    Retrieves the JavaScript source code of a deployed Worker script.\n\n    Args:\n        params: GetWorkerInput with script name\n\n    Returns:\n        Worker script source code\n    ",
      "maps_to": "cloudflare_get_worker",
      "server": "cloudflare-full",
      "inputSchema": {
        "properties": {
          "params": {
            "$ref": "#/$defs/GetWorkerInput"
          }
        },
        "required": [
          "params"
        ],
        "type": "object"
      }
    }
  }
}
