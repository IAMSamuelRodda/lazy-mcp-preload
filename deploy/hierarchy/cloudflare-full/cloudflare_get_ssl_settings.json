{
  "tools": {
    "cloudflare_get_ssl_settings": {
      "description": "Get SSL/TLS configuration including mode and certificate status.\n\n    Args:\n        params: GetSSLSettingsInput with zone\n\n    Returns:\n        SSL/TLS settings including mode, universal SSL status, and certificate info\n    ",
      "maps_to": "cloudflare_get_ssl_settings",
      "server": "cloudflare-full",
      "inputSchema": {
        "properties": {
          "params": {
            "$ref": "#/$defs/GetSSLSettingsInput"
          }
        },
        "required": [
          "params"
        ],
        "type": "object"
      }
    }
  }
}
