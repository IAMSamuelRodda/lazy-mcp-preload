{
  "tools": {
    "cloudflare_get_cache_settings": {
      "description": "Get zone cache configuration including level and browser TTL.\n\n    Args:\n        params: GetCacheSettingsInput with zone\n\n    Returns:\n        Cache settings including cache level, browser TTL, and other options\n    ",
      "maps_to": "cloudflare_get_cache_settings",
      "server": "cloudflare-full",
      "inputSchema": {
        "properties": {
          "params": {
            "$ref": "#/$defs/GetCacheSettingsInput"
          }
        },
        "required": [
          "params"
        ],
        "type": "object"
      }
    }
  }
}
