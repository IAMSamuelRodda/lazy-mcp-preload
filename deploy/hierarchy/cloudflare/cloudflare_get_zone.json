{
  "tools": {
    "cloudflare_get_zone": {
      "description": "Get details for a specific Cloudflare zone by name or ID.\n\n    Args:\n        params: GetZoneInput with zone name or ID\n\n    Returns:\n        Zone details including ID, status, nameservers, and plan\n    ",
      "maps_to": "cloudflare_get_zone",
      "server": "cloudflare",
      "inputSchema": {
        "properties": {
          "params": {
            "$ref": "#/$defs/GetZoneInput"
          }
        },
        "required": [
          "params"
        ],
        "type": "object"
      }
    }
  }
}
