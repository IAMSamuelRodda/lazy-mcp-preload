{
  "tools": {
    "cloudflare_delete_dns_record": {
      "description": "Delete a DNS record. This action cannot be undone.\n\n    Use cloudflare_list_dns_records first to find the record ID.\n\n    Args:\n        params: DeleteDNSRecordInput with zone and record ID\n\n    Returns:\n        Confirmation of deletion\n    ",
      "maps_to": "cloudflare_delete_dns_record",
      "server": "cloudflare",
      "inputSchema": {
        "properties": {
          "params": {
            "$ref": "#/$defs/DeleteDNSRecordInput"
          }
        },
        "required": [
          "params"
        ],
        "type": "object"
      }
    }
  }
}
