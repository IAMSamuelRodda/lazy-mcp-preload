{
  "tools": {
    "stalwart_queue_status": {
      "description": "\n    Get mail queue processing status. Shows if queue is running or paused.\n\n    Use to check if outgoing mail is being delivered or if queue is stopped.\n\n    Args:\n        params: QueueStatusInput containing:\n            - response_format: 'markdown' or 'json'\n\n    Returns:\n        Queue status (running/stopped).\n    ",
      "maps_to": "stalwart_queue_status",
      "server": "stalwart",
      "inputSchema": {
        "properties": {
          "params": {
            "$ref": "#/$defs/QueueStatusInput"
          }
        },
        "required": [
          "params"
        ],
        "type": "object"
      }
    }
  }
}
