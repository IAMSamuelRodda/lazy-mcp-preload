{
  "tools": {
    "stalwart_delete_account": {
      "description": "\n    Delete account permanently. Removes mailbox and all data. Cannot undo.\n\n    Args:\n        params: DeleteAccountInput containing:\n            - account_name: Account to delete (required)\n\n    Returns:\n        Confirmation that account was deleted.\n    ",
      "maps_to": "stalwart_delete_account",
      "server": "stalwart",
      "inputSchema": {
        "properties": {
          "params": {
            "$ref": "#/$defs/DeleteAccountInput"
          }
        },
        "required": [
          "params"
        ],
        "type": "object"
      }
    }
  }
}
