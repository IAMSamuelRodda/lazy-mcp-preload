{
  "tools": {
    "stalwart_queue_retry": {
      "description": "\n    Force immediate retry of queued message. Reschedules for immediate delivery.\n\n    Use to retry a failed message without waiting for next scheduled attempt.\n\n    Args:\n        params: QueueMessageActionInput containing:\n            - message_id: Queue message ID (required)\n\n    Returns:\n        Confirmation that message was rescheduled.\n    ",
      "maps_to": "stalwart_queue_retry",
      "server": "stalwart",
      "inputSchema": {
        "properties": {
          "params": {
            "$ref": "#/$defs/QueueMessageActionInput"
          }
        },
        "required": [
          "params"
        ],
        "type": "object"
      }
    }
  }
}
