{
  "tools": {
    "tplink_get_status": {
      "description": "Get router status including WAN, WiFi, and device counts.\n\n    Provides overview of router health: internet connection status,\n    WiFi network info, connected device counts, and firmware version.\n    Use for quick health checks and network overview.\n\n    Args:\n        params: Input parameters with response format option.\n\n    Returns:\n        Router status summary in requested format.\n    ",
      "maps_to": "tplink_get_status",
      "server": "tplink-router",
      "inputSchema": {
        "properties": {
          "params": {
            "$ref": "#/$defs/GetStatusInput"
          }
        },
        "required": [
          "params"
        ],
        "type": "object"
      }
    }
  }
}
