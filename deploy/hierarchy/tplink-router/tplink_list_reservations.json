{
  "tools": {
    "tplink_list_reservations": {
      "description": "List all DHCP address reservations (static IPs).\n\n    Shows devices with reserved/static IP addresses configured in the router.\n    Use this to verify reservations, check for conflicts, and document\n    the network's static IP assignments.\n\n    Args:\n        params: Input parameters with response format option.\n\n    Returns:\n        DHCP reservations list in requested format.\n    ",
      "maps_to": "tplink_list_reservations",
      "server": "tplink-router",
      "inputSchema": {
        "properties": {
          "params": {
            "$ref": "#/$defs/ListReservationsInput"
          }
        },
        "required": [
          "params"
        ],
        "type": "object"
      }
    }
  }
}
