{
  "tools": {
    "tplink_list_devices": {
      "description": "List all devices connected to the router with IPs and MACs.\n\n    Shows currently connected devices including hostnames, IP addresses,\n    MAC addresses, and connection type (wired/wireless). Essential for\n    network inventory and troubleshooting IP conflicts.\n\n    Args:\n        params: Input parameters with response format option.\n\n    Returns:\n        Connected devices list in requested format.\n    ",
      "maps_to": "tplink_list_devices",
      "server": "tplink-router",
      "inputSchema": {
        "properties": {
          "params": {
            "$ref": "#/$defs/ListDevicesInput"
          }
        },
        "required": [
          "params"
        ],
        "type": "object"
      }
    }
  }
}
